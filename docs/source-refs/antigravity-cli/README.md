# Antigravity CLI source-refs

Captured: 2026-08-19.
Local binary: `/Users/roo/.local/bin/agy` — changelog at `1.1.15`.

Official docs fetched with `web_fetch` from [antigravity.google/docs/cli](https://antigravity.google/docs/cli/overview/). Flag tables were cross-checked against `agy --help` on this machine. Where the site and the binary disagree, **the binary wins** and the mismatch is called out.

| File | What it is |
|------|------------|
| [overview.md](./overview.md) | Product overview (TUI vs Antigravity 2.0, shared harness) |
| [headless.md](./headless.md) | **Print mode** — `agy -p` / `--print` / `--prompt`. This is the adapter contract. |

Primary official page for print mode: [Headless mode](https://antigravity.google/docs/cli/headless).

Config root: `~/.gemini/antigravity-cli/` (`settings.json`, plugins, last-conversation cache).
